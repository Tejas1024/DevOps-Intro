# Kubernetes

A container orchestration tool that manages Docker containers.

Features:
- Auto-scaling (adjust number of containers based on demand)
- Load balancing (distribute traffic)
- Self-healing (restarts failed containers)

Example: If your app needs 100 users, Kubernetes runs 2 containers. If demand rises to 1000 users, Kubernetes automatically increases containers.
